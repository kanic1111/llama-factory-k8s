apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llama4-helm.fullname" . }}-test-connection"
  labels:
    {{- include "llama4-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llama4-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
